import React from "react";
import "./ListerProfile.css";

function Manage() {
    return (
        // This will allow the user to update or delete an existing post
        <div>..</div>

        // To be coded:
        // Show user's posts as a GALLERY of videos
        // BUTTON for DELETE or UPDATE for each post

        // Make DELETE or UPDATE calls to DB
        // SAVE in DB
    )
}

export default Manage;